import React from "react";

export default function SectorsTabs() {
  return (
    <div>
      Create sectors, for Low current (list services and when user clicks on it
      show full description with pics) and medical(just page contains info about
      the field)
    </div>
  );
}
